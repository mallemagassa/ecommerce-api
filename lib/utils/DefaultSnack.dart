import 'package:flutter/material.dart';

class DefaultSnack extends StatelessWidget {
  const DefaultSnack({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}